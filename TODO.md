# TODO - Auditoria Frontend ZapERP (correções seguras)

- [ ] Corrigir variáveis CSS ausentes em `src/chats/chatList.css` (`--cl-line-height`, `--cl-space-8`, `--cl-row-bg`).
- [ ] Adicionar fallback progressivo para layout crítico sem `:has(...)` em `src/chats/chatList.css`.
- [ ] Remover import não utilizado em `src/chats/chatList.jsx`.
- [ ] Revisar ajuste seguro de navegação interna sensível (alerta de reconexão) em `src/chats/chatList.jsx`.
- [ ] Validar com build de produção.
- [ ] Entregar relatório final de auditoria com veredito.

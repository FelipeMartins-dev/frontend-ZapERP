# Copie para `.env.development.local` (gitignored) e ajuste a porta do SEU backend local.
# Deve bater com `PORT` em `backend/.env` (ex.: 3000 ou 5000).

VITE_API_URL=http://localhost:5000

# Opcional: só a porta, se preferir não repetir a URL inteira
# VITE_BACKEND_PORT=5000
